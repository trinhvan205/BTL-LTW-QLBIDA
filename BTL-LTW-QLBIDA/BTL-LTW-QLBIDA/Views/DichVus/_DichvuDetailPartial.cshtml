@* @model BTL_LTW_QLBIDA.Models.Dichvu

<div class="dv-detail-wrapper p-3">

    <!-- TITLE -->
    <h5 class="fw-bold mb-3 text-primary d-flex align-items-center">
        <i class="bi bi-info-circle me-2"></i>
        Chi tiết dịch vụ @Model.Tendv
    </h5>

    <!-- IMAGE -->
    <div class="text-center mb-3">
        <img src="@Model.Imgpath"
             class="rounded shadow-sm border"
             style="width:140px;height:140px;object-fit:cover;" />
    </div>

    <!-- INFO TABLE -->
    <table class="table table-bordered small">
        <tr>
            <th style="width:130px;">Mã DV</th>
            <td>@Model.Iddv</td>
        </tr>

        <tr>
            <th>Tên dịch vụ</th>
            <td>@Model.Tendv</td>
        </tr>

        <tr>
            <th>Loại</th>
            <td>@Model.IdloaiNavigation?.Tenloai</td>
        </tr>

        <tr>
            <th>Giá bán</th>
            <td class="fw-bold text-primary">
                @((Model.Giatien ?? 0).ToString("N0")) VNĐ
            </td>
        </tr>

        <tr>
            <th>Tồn kho</th>
            <td class="fw-bold @(Model.Soluong <= 5 ? "text-danger" : "")">
                @Model.Soluong
            </td>
        </tr>

        <tr>
            <th>Trạng thái</th>
            <td>
                @if (Model.Hienthi == true)
                {
                    <span class="badge bg-success px-3 py-2">Đang bán</span>
                }
                else
                {
                    <span class="badge bg-secondary px-3 py-2">Ngưng bán</span>
                }
            </td>
        </tr>
    </table>

    <!-- ACTION BUTTONS -->
    <div class="d-flex justify-content-end mt-3 gap-2">
        <button class="btn btn-primary btn-sm btn-edit-detail"
                data-id="@Model.Iddv">
            <i class="bi bi-pencil-square"></i> Sửa
        </button>

        <button class="btn btn-danger btn-sm btn-delete-detail"
                data-id="@Model.Iddv">
            <i class="bi bi-trash"></i> Xóa
        </button>
    </div>

</div>

<style>
    .dv-detail-wrapper table th {
        background: #f8f9fa;
        width: 130px;
    }
</style> *@
